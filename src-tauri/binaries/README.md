# Linux note

Linux builds no longer bundle `ffmpeg` / `ffprobe` sidecars.
The app uses the system-provided binaries from `PATH` instead.

This directory is kept only for any future non-Linux packaging needs.
